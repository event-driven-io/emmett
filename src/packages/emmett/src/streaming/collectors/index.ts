import { collect } from './collect';
import { first, firstOrDefault } from './first';
import { single, singleOrDefault } from './single';

export const streamCollectors = {
  collect,
  first,
  firstOrDefault,
  single,
  singleOrDefault,
};
